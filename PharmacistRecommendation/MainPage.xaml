<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="PharmacistRecommendation.MainPage"
             Title="Pharmacist Recommendation System">
    <ScrollView>
        <VerticalStackLayout Padding="30,0" Spacing="25">

            <Image
                Source="dotnet_bot.png"
                HeightRequest="185"
                Aspect="AspectFit" />

            <Label
                Text="Pharmacist Recommendation System"
                Style="{StaticResource Headline}"
                SemanticProperties.HeadingLevel="Level1" />

            <Label
                Text="Welcome to &#10;Recomandarea Farmacistului"
                Style="{StaticResource SubHeadline}"
                SemanticProperties.HeadingLevel="Level2" />

            <!-- Main Feature - Medication Management -->
            <Button
                x:Name="MedicationBtn"
                Text="📋 Configurari Medicamente"
                BackgroundColor="DarkBlue"
                TextColor="White"
                FontSize="18"
                Clicked="OnMedicationClicked"
                HorizontalOptions="Fill" />

            <!-- Placeholder buttons for colleague's work -->
            <Button
                Text="🏥 Monitorizare Pacienti (Coming Soon)"
                BackgroundColor="Gray"
                TextColor="White"
                IsEnabled="False"
                HorizontalOptions="Fill" />

            <Button
                Text="📄 Emitere Documente (Coming Soon)"
                BackgroundColor="Gray"
                TextColor="White"
                IsEnabled="False"
                HorizontalOptions="Fill" />

            <Button
                Text="📊 Rapoarte (Coming Soon)"
                BackgroundColor="Gray"
                TextColor="White"
                IsEnabled="False"
                HorizontalOptions="Fill" />

            <!-- System Tools -->
            <Label Text="System Tools" FontSize="16" FontAttributes="Bold" Margin="0,20,0,0"/>

            <Button
                x:Name="TestDbBtn"
                Text="Test Database Connection"
                BackgroundColor="Green"
                TextColor="White"
                Clicked="OnTestDbClicked"
                HorizontalOptions="Fill" />

            <Button
                x:Name="TestMedicationBtn"
                Text="Test Medication Data"
                BackgroundColor="Orange"
                TextColor="White"
                Clicked="OnTestMedicationClicked"
                HorizontalOptions="Fill" /> 

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>